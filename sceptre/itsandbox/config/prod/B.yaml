# Instance to test integration with parkmycloud
template_path: noop.yaml
stack_name: B
dependencies:
  - prod/A.yaml
parameters:
  MyId: "B"

#parameters:
#  CanonicalUserId: !rcmd "export AWS_PROFILE=sandbox-admin && aws s3api list-buckets --query Owner.ID --output text"
#  CanonicalUserId: !rcmd "aws s3api list-buckets --query Owner.ID --output text"
#  CanonicalUserId: !rcmd
#    command: "aws s3api list-buckets --query Owner.ID --output text"
#    profile: "sandbox-admin"
